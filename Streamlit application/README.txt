APP.PY = INITIAL WORKING app
Main.py = FINAL running file which works perfectly and has all the models
Workingapp.py = This contains other methods which were tried for the app
templates = It has all the templates and CSS files required for the styling of the web app


TO RUN THE FILE:
1. You must open the cmd in this location where all the files are present
2. Then run 'Streamlit run Main.py' 
3. It will automatically open in your default browser